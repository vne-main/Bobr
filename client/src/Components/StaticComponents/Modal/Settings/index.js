import React from 'react';
import Modal from '../hoc';

const ModalSettings = () => {
	return (
		<div>
			<h3 className="modal__header modal__padding">В разработке</h3>
		</div>
	);
};

export default Modal(ModalSettings);
